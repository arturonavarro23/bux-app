const palette = {
  common: {
    main: '#ff3c64',
    black: '#222222',
    white: '#fff',
  },
  error: {
    main: '#A51C30',
    light: '#A7333F',
  },
  borderColor: 'rgb(237, 242, 247)',
};

const typography = {
  fontFamily: ['"Muli"', 'sans-serif'].join(', '),
  fontSize: '1rem',
};

const theme = {
  typography,
  palette,
};

export default theme;
